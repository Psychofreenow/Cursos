Con este comando inicialiazamos git en nuestro proyecto. Nos crea un repositorio.

git init

Nota: el repositorio se llama .git, lo significa que va a estar oculto el archivo. Para mostrarlo utilizamos el comando:

ls -a